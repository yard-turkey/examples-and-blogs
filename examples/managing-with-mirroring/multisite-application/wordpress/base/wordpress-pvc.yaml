apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wp-pv-claim
  labels:
    app: wordpress
spec:
  storageClassName: rook-ceph-block-demo3
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
